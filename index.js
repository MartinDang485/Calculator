const allNums = document.querySelectorAll('[data-number]');
const allOperations = document.querySelectorAll('[data-operation]');

allNums.forEach(button => {
    button.addEventListener(onclick, x)
})
